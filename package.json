{
  "name": "puzzleproject",
  "version": "1.0.0",
  "description": "Small project for Nicolay's birthday",
  "scripts": {
    "start": "parcel src/index.html",
    "clean": "rm -rf ./.parcel-cache && rm -rf ./dist && rm -rf ./docs",
    "build": "npm run clean && parcel build src/index.html --dist-dir docs --public-url ./"
  },
  "dependencies": {
    "parcel": "^2.0.1"
  }
}