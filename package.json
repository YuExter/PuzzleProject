{
  "name": "puzzleproject",
  "version": "1.0.0",
  "description": "Small project for Nicolay's birthday",
  "scripts": {
    "dev": "parcel src/index.html",
    "build": "parcel build src/index.html --public-url /PuzzleProject/ --no-content-hash",
    "deploy": "gh-pages -d dist",
    "predeploy": "npm run build"
  },
  "dependencies": {
    "gh-pages": "^3.2.3",
    "parcel": "^2.0.1"
  },
  "devDependencies": {}
}
